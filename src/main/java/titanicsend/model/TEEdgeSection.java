package titanicsend.model;

public enum TEEdgeSection {
    // For cart orientation, see
    // https://github.com/titanicsend/LXStudio-TE/blob/main/assets/vehicle-axes-orientation.png

    // All edges on the front face of the cart (stage left)
    PORT,
    // All edges on the back face of the cart (stage right)
    STARBOARD,
}
